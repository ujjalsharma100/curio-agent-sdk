# =============================================================================
# MULTI-PROVIDER WITH FAILOVER
# =============================================================================
# Production setup with multiple providers for reliability.
# The order in TIER*_MODELS IS the priority - if first fails, try second, etc.
# You can mix providers in any order!
# =============================================================================

# API Keys - set all providers you want to use
OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here
GROQ_API_KEY=gsk_your-groq-key-here

# -----------------------------------------------------------------------------
# Tier 1: Fast/Cheap - for synthesis, simple lookups
# Try Groq first (fastest), fall back to OpenAI mini
# -----------------------------------------------------------------------------
TIER1_MODELS=groq:llama-3.1-8b-instant,openai:gpt-4o-mini

# -----------------------------------------------------------------------------
# Tier 2: Balanced - for general reasoning
# Try Groq 70B first, then OpenAI, then Anthropic
# -----------------------------------------------------------------------------
TIER2_MODELS=groq:llama-3.3-70b-versatile,openai:gpt-4o,anthropic:claude-3-5-sonnet-20241022

# -----------------------------------------------------------------------------
# Tier 3: Best Quality - for planning, complex reasoning
# Try Anthropic first (best quality), fall back to OpenAI, then Groq
# -----------------------------------------------------------------------------
TIER3_MODELS=anthropic:claude-3-5-sonnet-20241022,openai:gpt-4o,groq:llama-3.3-70b-versatile

# =============================================================================
# EXAMPLE: Same provider, multiple models as fallback
# =============================================================================
# You can also list multiple models from the same provider:
#
# TIER3_MODELS=openai:gpt-4o,openai:gpt-4-turbo,openai:gpt-4o-mini

# =============================================================================
# MULTI-KEY ROTATION (Optional)
# =============================================================================
# If you have multiple API keys for a provider (to avoid rate limits),
# configure them with numbered suffixes. SDK rotates through healthy keys.
# =============================================================================

# Multiple Groq keys for high-volume usage
# GROQ_API_KEY_1=gsk_first-key-here
# GROQ_API_KEY_1_NAME=primary
# GROQ_API_KEY_2=gsk_second-key-here
# GROQ_API_KEY_2_NAME=secondary
